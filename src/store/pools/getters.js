export const getPools = ({ pools }) => pools;
export const getUserPools = ({ userPools }) => userPools;
export const isLoading = ({ isLoading }) => isLoading;
export const getConfig = ({ config }) => config;
